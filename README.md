# aws_fargate_scheduled_task_multicontainer
Terraform module for a scheduled task running in fargate. The task can run multiple containers and one CloudWatch log group is provisioned for each container. 
